# Legacy Content

This folder contains archived source material and one-off migration scripts. The canonical guide content lives under `guide/`.

- `legacy/chapters/` holds the original chapter drafts.
- `legacy/appendices/` holds the original appendices and study aids.
- `legacy/scripts/` holds one-time migration scripts that are no longer in active use.
